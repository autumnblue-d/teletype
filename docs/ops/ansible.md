## Ansible

The `KR.*` ops drive the **on-board native Kria** sequencer (not an external
Ansible over i2c) in this firmware — track and parameter indices are 0-based
unless noted. The `ANS.*` ops (grid / arc / app) still address an external
Ansible module over i2c.

